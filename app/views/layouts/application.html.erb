<!DOCTYPE html>
<html>
  <head>
    <title>Elevate</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script src="https://kit.fontawesome.com/9b8a7e18a0.js"></script>
  </head>

  <body>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= render "shared/navbar" %>
  <% if current_user %>
    <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete},  class: "btn btn-secondary logout-button" %>
  <% end %>
    <div class="container">
    <div style="margin-bottom: 70px"> <%# margin is the same as navbar height  %>
      <%= yield %>
    </div>
  </body>
</html>
