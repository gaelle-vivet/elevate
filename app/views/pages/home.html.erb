<div class="homepage" <%= image_tag "bk-img.jpg", id:"bk-img" %>
  
  <div class="head">
    <h1 class="d-none">Elevate</h1>
    <%= image_tag "elevate_logo.png", id:"logo" %>
      <p class="slogan">Find your favorite Teacher <br> close to you</p>
  </div>
  <div class="flex-container item-flex">
  <!-- First Part: Dropdown Menu -->
    <%# <div class="flex-item lessons-select"> %>
    <%= form_with url: root_path, method: :get, class: "d-flex text_and_icon" do %> 
      <%= select_tag "topic", options_for_select(["English", "Geo", "Math"]), #Lesson.all.pluck(:subject).uniq
            class: "form-control flex-item-grow",
            placeholder: "What do you want to learn today?"
        %>
            <div class="line"></div>

      <%= submit_tag "", name: "search", class: "btn flex-item text_and_icon" %>
      <i class="fas fa-search search_icon"></i>    
    <% end %>
      <%# <select class="flex-item-grow lessons-select">
        <option value="option1">Math</option>
        <option value="option2">Geography</option>
        <option value="option3">Coding</option>
        <!-- Add more options as needed -->
      </select> %>
    <%# </div> %>
     

    <!-- Second Part: City Location Input -->
    <%# <div style="flex: 2;">
      <input type="text" placeholder="Enter your city">
    </div> %>

    <!-- Third Part: Search Button -->
    <%# <div class="flex-item text_and-icon">
      <i class="button fas fa-search search_icon"></i>    
    </div> %>
  </div>
  <div class="bottom">
    <p class="slogan" >Empowering Minds Elevating Futures</p>
  </div>

</div>